DATABASE_URL="postgresql://als:als_pass@localhost:5432/alsapp"
JWT_SECRET="change-me"
OPENWEATHER_API_KEY=your-openweather-api-key
GROQ_API_KEY=your-groq-api-key
# Base64-encoded Firebase service account JSON (for push notifications) -
# get one at https://console.firebase.google.com under Project settings ->
# Service accounts -> Generate new private key, then base64-encode the file.
FIREBASE_SERVICE_ACCOUNT_BASE64=
PORT=8080